PolyCode Python Engine
======================
Dependencies for the PolyCode collaborative Python REPL engine.

Usage:
    pip install -r requirements.txt
    python main.py

The server listens on 127.0.0.1:9765 for WebSocket (JSON-line) connections.
